<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>edit listing</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container py-4">
        <h2>edit Listing</h2>
        <form action="/listings/<%= list._id %>?_method=PUT" method="POST">
            <div class="mb-3">
                <label for="title" class="form-label">Title</label>
                <input type="text" name="listings[title]" value="<%= list.title %>" id="title" required class="form-control">
            </div>
            <div class="mb-3">
                <label for="des" class="form-label">Description</label>
                <textarea name="listings[des]" id="des" required class="form-control"><%= list.des %></textarea>
            </div>
            <div class="mb-3">
                <label for="price" class="form-label">Price</label>
                <input type="number" name="listings[price]" value="<%= list.price %>" id="price" required class="form-control">
            </div>
            <div class="mb-3">
                <label for="state" class="form-label">State</label>
                <input type="text" name="listings[state]" value="<%= list.state %>" id="state" required class="form-control">
            </div>
            <div class="mb-3">
                <label for="country" class="form-label">Country</label>
                <input type="text" name="listings[country]" value="<%= list.country %>" id="country" required class="form-control">
            </div>
            <div class="mb-3">
                <label for="image" class="form-label">Image (Upload a file or paste URL)</label>
                <input type="file" name="listings[imageFile]" id="imageFile" class="form-control mb-2">
                <input type="url" name="listings[img]" id="img" placeholder="Or paste image URL" class="form-control">
            </div>
            <button type="submit" class="btn btn-primary">Save</button>
        </form>
    </div>
</body>
</html>
